name: copier
description: Package for copy files, directories, links. You can watch the copying process.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=2.12.0"

topics:
  - files
  - copy

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
